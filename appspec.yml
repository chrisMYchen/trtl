version: 0.0
os: linux
files:
  - source: /
    destination: /var/trtl/
hooks:
  ApplicationStart:
    - location: deployment/start_server.sh
      timeout: 300
      runas: root
  ApplicationStop:
    - location: deployment/stop_server.sh
      timeout: 300
      runas: root
